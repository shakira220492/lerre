{{ form_start(form_increase_amount_view_video_form_ajax, { 'attr' : {'novalidate': 'novalidate', 'role' : 'form', 'id': id_increase_amount_view_video_form_ajax|default('form')} }) }}

{{ form_end(form_increase_amount_view_video_form_ajax) }}
<div style="
     background-color: #c9302c; 
     position: absolute;
     top: 0px;
     left: 0px;
     width: 215px;
     height: 35px;">
    <input type="text" id="keyword" placeholder="JBSB92" name="keyword" {#size="19"#}
           style="width: 215px;
           height: 35px;"/>
</div>
<div class="searchProduct" style="
     background-color: #1ab7ea; 
     position: absolute;
     top: 0px;
     left: 215px;
     width: 35px;
     height: 35px;">
</div>

<script>
    $(".searchProduct").click(function ()
    {
        alert("ATENTTION!");
        
        var keyword = document.getElementById('keyword').value;

        $.post("reloadListSearchProduct", {keyword: keyword}, function (htmlExterno) {
            $(".divListSongs").html(htmlExterno);
        });
        console.log(keyword);
    });

    function increaseAmountViewVideo(ProductId)
    {
        var form = $('#increase-amount-view-video-ajax');

        var url = form.attr('action');

        var id_product = ProductId;


        $.post(url, {id_product: id_product
        }, function (result) {
            console.log("product_id: " + result[0].product_id);
            console.log("product_amountVisits: " + result[0].product_amountVisits);

        }).fail(function () {
            alert('ERROR');
        });
    }
</script>